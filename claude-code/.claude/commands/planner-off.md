---
description: Turn off planner-guard for this session so the main agent can edit code directly
---

The planner-guard for this session has been turned OFF (the
UserPromptSubmit hook removed the session lock). The main agent may now
edit code directly again.

Do not take any action from this command by itself — just acknowledge
that direct editing is re-enabled, and continue with whatever the user
asks next.
